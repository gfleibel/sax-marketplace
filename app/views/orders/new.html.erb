<div class='container'>
  <h1>Confirme sua compra</h1>
  <p><%= @order.saxophone %></p>

  <%= simple_form_for [@saxophone, @order], url: saxophone_orders_path(@saxophone), method: :post do |f| %>
    <%= f.hidden_field :user_id, value: current_user.id %>
    <%= f.hidden_field :saxophone_id, value: @saxophone.id %>
    <%= f.submit %>
  <% end %>

</div>
